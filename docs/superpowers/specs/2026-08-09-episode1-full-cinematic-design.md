# Episode 1 Full Cinematic Design

## Approved direction
Build the full **What If Humans Never Needed Sleep?** episode using the cinematic visual language from the pre-editorial benchmark the user approved. Do not use the editorial-collage look as the final episode style.

## Visual architecture
- Replace the rejected SVG/doodle beat renderer with one cinematic visual owner per beat.
- Keep a continuously painted base plate so there are no black timeline gaps.
- Use the approved benchmark's photographic/cinematic treatment as the hero language, supplemented by authored cinematic motion-graphic scene families for biology, circadian time, economy, boundaries, home life, psychology, and payoff sections.
- Change composition/motif every 3–6 seconds using the existing measured beat timeline.
- Use restrained typography only for meaningful numbers or short paradox phrases.
- Keep captions mobile-readable and subordinate to the image.
- Never use Higgsfield.

## Narration
- Lock Kokoro `af_heart` at speed `0.95` with no fallback.
- Generate each scene as continuous prose, not sentence-by-sentence clips.
- Trim only true leading/trailing silence from each generated scene; do not insert sentence gaps.
- Reject any other narrator identity before rendering.

## QA and publishing
- Verify full-frame visual coverage, short-black-frame detection, audio presence, voice identity, dimensions, duration, contact sheets, and representative frames across every section.
- Independent QA must score every core gate >=9/10 with zero major blockers before publishing.
- Use the already-approved verified ChatGPT Images thumbnail override.
- Upload only the exact approved render PRIVATE, verify processing/title/thumbnail/playback/privacy, persist the real video ID/link, and leave rejected video `3EGJqkrn42A` private.
