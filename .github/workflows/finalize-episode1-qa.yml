name: Retired stale Episode 1 QA finalizer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse synthetic QA pass
        run: |
          echo "This workflow is retired. QA may only be written after independent review of the exact new render artifact."
          exit 1
