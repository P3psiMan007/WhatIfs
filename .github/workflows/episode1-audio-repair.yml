name: Retired stale Episode 1 audio repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse stale-artifact repair
        run: |
          echo "This workflow is retired. It referenced a superseded render and must not alter the current episode."
          exit 1
